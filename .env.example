# AutoTrowel - Environment Variables Configuration Template
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control!

# ==============================================================================
# Database Configuration (for AutoTrowel_Documented.py)
# ==============================================================================

# Option 1: Individual components (recommended)
DB_SERVER=your_sql_server_hostname
DB_NAME=your_database_name
DB_USER=your_username
DB_PASSWORD=your_secure_password
DB_DRIVER=ODBC Driver 17 for SQL Server
DB_PORT=1433

# Option 2: Complete connection string (overrides individual settings)
# DB_CONNECTION_STRING=mssql+pyodbc://username:password@server:1433/database?driver=ODBC+Driver+17+for+SQL+Server

# Database Table Configuration
DB_TABLE_NAME=IceAssets

# ==============================================================================
# Database Configuration (for ice_scraper.py)
# ==============================================================================

# Use ICE_ prefix for ice_scraper.py to avoid conflicts
ICE_DB_SERVER=your_sql_server_hostname
ICE_DB_NAME=your_database_name  
ICE_DB_USER=your_username
ICE_DB_PASSWORD=your_secure_password
ICE_DB_DRIVER=ODBC Driver 17 for SQL Server
ICE_DB_PORT=1433

# Or use complete connection string
# ICE_DB_CONNECTION=mssql+pyodbc://username:password@server:1433/database?driver=ODBC+Driver+17+for+SQL+Server&trusted_connection=yes

# ==============================================================================
# API Configuration (optional overrides)
# ==============================================================================

# API_TIMEOUT=30
# API_MAX_RETRIES=3
# API_PAGE_SIZE=1000

# ==============================================================================
# Logging Configuration
# ==============================================================================

LOG_LEVEL=INFO
LOG_DIR=logs

# ==============================================================================
# Security Notes
# ==============================================================================
#
# 1. After copying to .env, replace all 'your_*' placeholders with actual values
# 2. Use strong passwords (minimum 12 characters, mixed case, numbers, symbols)
# 3. Never commit .env file to Git
# 4. For production, consider using Azure Key Vault or similar secret management
# 5. For Windows Authentication, you can leave DB_USER and DB_PASSWORD empty
#
